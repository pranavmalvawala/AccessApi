# Modify and save this file as ".env"
API_ENV=dev
APP_NAME=AccessApi

# Node.js server configuration
SERVER_PORT=8082

# MySql configuration
DB_HOST=localhost
DB_DATABASE=access
DB_USER=root
DB_PASSWORD=

JWT_SECRET_KEY=this is a test key that is a sentence
JWT_EXPIRATION=2 days

SUPPORT_EMAIL=support@churchapps.org

# Supports SES/SMTP. In case of no or incorrect value, SMTP will be used by default
MAIL_SYSTEM=SMTP

# Frontend
FRONTEND_HOST=https://accounts.staging.churchapps.org